<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="HeriStepAI.Mobile.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:HeriStepAI.Mobile.Views"
    Shell.FlyoutBehavior="Disabled">

    <TabBar>
        <ShellContent
            Title="Bản đồ"
            ContentTemplate="{DataTemplate views:MapPage}"
            Icon="{OnPlatform Android=map, iOS=map}"
            Route="map" />

        <ShellContent
            Title="Điểm"
            ContentTemplate="{DataTemplate views:POIListPage}"
            Icon="{OnPlatform Android=location, iOS=location}"
            Route="pois" />

        <ShellContent
            Title="Tour"
            ContentTemplate="{DataTemplate views:TourListPage}"
            Icon="{OnPlatform Android=route, iOS=route}"
            Route="tours" />

        <ShellContent
            Title="Cài đặt"
            ContentTemplate="{DataTemplate views:SettingsPage}"
            Icon="{OnPlatform Android=settings, iOS=settings}"
            Route="settings" />
    </TabBar>

</Shell>
